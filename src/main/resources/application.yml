server:
  port: 8088

spring:
  application:
    name: lms-config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config